package edu.byui.apj.week1;

public class FibonacciSequenceWithTwist {
    public static void main(String[] args) {
        // Get the length of how many Fibonacci numbers to create
        try {
            int phraseCount = Integer.parseInt(args[0]);
        } catch (NumberFormatException e) {
            System.out.println("InputFormatError. Please enter a number");
        }

        }
        else {

        }

        if (phraseCount < 2) {
            for (int i = 0; i < phraseCount; i++) {
                int firstNum = 0;
                int secondNum = 1;

            }
        } else if (phraseCount == 2) {
            System.out.println("0*, 1");

        }
        else if (phraseCount == 1) {
            System.out.
        }

    }
}
